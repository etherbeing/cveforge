def triangle_encode(number: int):
    # y = number # or adjacent cathetus
    # angle_abc = 90
    # angle_acb = 30
    # angle_bac = 60
    # m = 0 # the pendient or also the side that is opposed to the rectangle angle.
    # As we are treating this both as a side or as the pendient of a function it has different ways to be calculated:
    # 1. (y1-y0)/(x1-x0)
    # 2. h**2=a**2+b**2
    # 3. ...
    # (h**2-a**2)**(1/2)=b
    #
    # Using the sine function we know the sin of 30 is 1/2 meaning co/h it has a relation of 1/2 meaning co = h/2
    pass
